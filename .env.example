# Admin panel password for HTTP Basic Auth (username is always "admin")
ADMIN_PASSWORD=changeme

# HTTP port the server listens on
PORT=3000

# OpenAI API key for AI chat fallback (optional — AI disabled if unset)
OPENAI_API_KEY=sk-...
